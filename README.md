# LICATAN-EDUARD-JEUS-AUTOMATA
This program implements an NFA (Nondeterministic Finite Automaton) that checks whether an input is a valid C-style comment. A valid comment must start with /* and end with */. The program allows different characters inside the comment, except that a / or * must follow the proper pattern for the comment to be valid.
